# Git Webhook JaCoCo API 环境配置示例
# 复制此文件为 .env 并根据您的环境进行配置

# Git Webhook 密钥 - 在 GitHub/GitLab 仓库设置中配置的 Secret
# 设置为 "your_default_secret_token" 可跳过签名验证（仅用于测试）
GIT_WEBHOOK_SECRET=your_default_secret_token

# 调试模式 (开发时设置为 True，生产环境设置为 False)
DEBUG=True

# 日志级别 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Redis 配置 (用于 Celery 任务队列)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
