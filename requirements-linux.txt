# Python 3.12+ 兼容的依赖列表
# 专门为 Linux 环境和新版本 Python 优化

# 核心 Web 框架
fastapi==0.104.1
uvicorn[standard]==0.23.2

# 环境配置
python-dotenv==1.0.0
pydantic==2.4.2
pydantic-settings==2.1.0

# 任务队列
celery==5.3.4
redis==5.0.1

# HTTP 客户端
requests==2.31.0
httpx==0.25.1

# 文件上传支持
python-multipart==0.0.6

# 监控和日志
prometheus-client==0.17.1
python-json-logger==2.0.7

# 测试框架
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Python 3.12+ 兼容性修复
setuptools>=68.0.0
wheel>=0.41.0

# 避免使用有问题的 docker 库，改用 subprocess 调用
# docker==7.1.0  # 注释掉，改用 subprocess

# XML 处理（可选，如果需要高级 XML 处理）
# lxml==4.9.3  # 注释掉，使用内置的 xml.etree.ElementTree

# 其他工具
pathlib  # 内置模块，不需要安装
